# AI 工牌 — 05 规约对抗审查报告

| 项 | 内容 |
|---|---|
| 版本 | v1.0(首次审查) |
| 审查范围 | `01-需求文档.md`、`02-Agent需求文档.md`、`03-项目宪法.md`、`04-开发规约.md` |
| 方法 | 逐条交叉比对:FR ↔ 场景流程 ↔ 红线 ↔ 非功能指标;专找三类问题——自相矛盾、二义表述、不可验收条款 |

---

## 1. 发现与处理(全部已修正)

| # | 级别 | 发现 | 处理 | 状态 |
|---|---|---|---|---|
| F1 | **高** | 时间线矛盾:02 §8 按 14 周排期(人类团队手速),与 2026-07-19 评审确认的两人团队 4~6 周口径冲突 | 02 §8 改为意图概述 + 指针;权威计划独立为 `06-开发计划.md` | ✅ 已修正 |
| F2 | 中 | 模式选择双机制:01 §4.1 用"长按=现场记录"手势,02 FR-04 用"录音前模式切换",两者优先级未定义,固件与软件可能各做一套 | 统一为:**显式模式优先于自动路由;长按是模式指定的快捷方式,语义等价**。01 §4.1、02 FR-04 已对齐 | ✅ 已修正 |
| F3 | 中 | 简报触发二义:01 §4.2 暗示"到岗连接时生成",02 FR-06 写"定时 08:30 生成",PC 晚开机/未连接时行为未定义 | FR-06 明确三态:在线即推 / 暂存待补发 / 晚开机首次连接补生成 | ✅ 已修正 |
| F4 | **高** | 02 §7 允许演示期第三方 LLM API,与"数据不出受控环境"原则冲突,且原文"需脱敏"无强制力 | 上升为宪法第 3 条**受控例外**,四项强制条件:显式开启、文本脱敏、审计标记、企业阶段关闭;02 §7 同步改写 | ✅ 已修正 |
| F5 | 低 | 虚拟工牌兼容口径不一:FR-12 正文"近 5 年 Android Chrome" vs 验收"Android 9+"(Android 9 为 2018 年) | 统一为 Android 9+ 的 Chrome | ✅ 已修正 |
| F6 | 中 | 01 v0.2 方案 A 整机尺寸(≤99×85mm 横宽)与 2026-07-20 Owner 决策(工牌外形固定 60×90mm 竖向、屏幕模组与外形分离)不一致;01 §4 场景多处"回身份页"与新页面模型(默认页=最高优先提醒,结果页返回原页面)亦需同步 | 交互层以 08 §6 新契约为准(已重写);01 由硬件修订方在下一版同步 | ⏳ 待 01 同步 |

## 2. 抽查通过项(验证一致,抽样)

- FR-02"<2 秒录音静默丢弃" ↔ 01 §4.5 通用异常:一致
- FR-07 提醒卡片仅两类(待办/定时)↔ 01 §3.3-3 红线:一致
- FR-08 白名单三指令 ↔ 01 §3.1-9"2~3 个高频指令":一致
- FR-09 L2 确认回路 ↔ 01 §4.3 T5' 确认分支(15 秒超时取消):一致
- FR-08 幂等键 record_id ↔ 02 §3.3 `audio.upload` 携带 record_id:一致
- 音频转写后默认删除 ↔ 01 §3.3-8 ↔ 宪法第 3 条:三方一致
- 端-云协议消息集合覆盖四个场景流程的全部交互步骤:无缺口

## 3. 遗留观察项(不阻塞,转入待决)

| # | 观察 | 处置 |
|---|---|---|
| O1 | MVP 为单用户单设备;试点阶段(30+ 人)的多用户/多设备模型当前无定义 | A3 阶段开工前补一份多用户模型需求,走文档变更程序 |
| O2 | 演示期 LLM 选型(第三方 API vs 本地模型)未定 | A0-5 实测报告支撑,Owner 拍板 |
| O3 | 企业 ASR 是否有合规指定未知 | 适配层已隔离,影响限于新增一个 adapter |

## 4. 程序约定

- 本报告为首次对抗审查。此后**每次任一规约文档大版本变更**,需重跑交叉审查并追加报告版本。
- 审查中发现的红线级问题(涉及宪法),必须当场修正文档,不允许"留待实现时处理"。
